# Because who needs to type accurately?
alias cd..='cd ..'
alias fidd='diff'
alias insatll=install
alias isntall=install
alias mdkir='mkdir'
alias mdkri='mkdir'
alias pus='push'
alias branc='branch'

# Because I won't remember these commands
alias zs="vim ~/.zshrc"
alias sz="source ~/.zshrc"

# Because Vim
alias Z="exiter"
alias ZZ="exiter"
alias ZZZ="exiter"
alias jj="pwd"
alias o="open_this_folder"

# Because lazy
alias b="xdg-open"
alias c="clear"
alias cath="head -10"

# shortcut for history | grep 'string'
alias f="find_in_history"
alias h="find_in_history"

# alias co="checker_outer"
alias cd="cd_and_chill" # allows for opening most recent folder with  each new instance

alias ga="git add"
alias gb="git_brancher"
alias c="git_options"

# alias gdf="git difftool"
alias gp="git pull --recurse-submodules"
alias gpsh="git push"
alias grf="git show --pretty='' --name-only HEAD" # git recent files
alias gst="git status"
alias hsk='stack ghci'
alias lg="ls --color=auto && gst"
alias ll="ls -lhA"
alias killjobs="kill ${${(v)jobstates##*:*:}%=*}"
alias mkdircd="mkdir_and_cd"
#alias ngs="npm run NG_PERSISTENT_BUILD_CACHE=1 && node --max_old_space_size=8096 ./node_modules/@angular/cli/bin/ng serve",
alias ngso="ng serve --open"
alias ngs="ng serve"
alias py="python3"
alias pytest="pytest -rs"
alias python="python3"
alias pycov="python_generate_coverage"
alias rf="rm -rf"
alias v="open_last_git_commits_and_most_recent_directory"
alias ..='down_n_folder'
alias ...='cd'
alias pips="pip_install_save"
alias ports="find_by_port_no"
alias killport="find_and_kill_port"
alias prs="python ~/PR-selector/update_PR.py"
alias gcb="git_checkout_and_switch_to_branch"
alias gcl="git_clean_branches"
alias restart="reboot"
alias update="sudo pacman -Syyu"

# docke
alias dst="docker_stop_instance"
alias dps="docker_names_only"
alias dup="docker-compose up"
alias dcu="docker-compose up"
alias dcl="docker_clean"
alias dex="docker_explore_container"
alias dim='docker images --format "{{.Repository}}"'
alias dn='docker_network_name'

# Nice colours for grep
alias grep='grep --color=auto'

# nice colours for git log
alias glg="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --"

# displays my ip
alias myip="dig +short myip.opendns.com @resolver1.opendns.com"

# can never remember perl regex syntax
alias rgx="perl_regex"

# side by side diff
alias dff="side_by_side_diff"

# work specific
alias work="cd ~/RAP"
alias jenkins="ssh -L 8080:localhost:8080 jenkins"

